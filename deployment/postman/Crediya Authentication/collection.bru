meta {
  name: Crediya Authentication
}

auth {
  mode: none
}

vars:pre-request {
  baseUrl: http://localhost:8080
  userId: u-1
}

docs {
  Postman collection for Crediya Authentication service (WebFlux). Includes user creation and retrieval endpoints.
}
